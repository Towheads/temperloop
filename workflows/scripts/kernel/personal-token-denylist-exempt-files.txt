# personal-token-denylist-exempt-files.txt — files WHOLESALE exempted from
# check-personal-token-denylist.sh (foundation #798), one git-relative path
# per line. `#` starts a comment; blank lines ignored.
#
# Every entry here is a fixture-replay TEST that exercises the boards.conf
# built-in fallback map (workflows/scripts/board/lib/board.sh,
# workflows/scripts/build/funnel-tick.sh, workflows/scripts/build/funnel-drive.sh
# — see those files' own `# denylist:allow` markers) with literal mock `gh`
# JSON/CLI-call data matching that map's real values. Scrubbing the literal
# strings in these tests would not remove anything the already-exempted
# runtime default doesn't already disclose (it's synthetic fixture data, not
# a live credential), and would be a large, error-prone rewrite (dozens of
# assertions per file) for zero privacy benefit — see the #798 build's
# verification doc for the full scoping rationale. A NEW personal-token class
# introduced in one of these files (not just the existing board fallback
# values) would NOT be caught by this blanket exemption today — narrow it to
# per-line `# denylist:allow` markers if that becomes a live risk.

workflows/scripts/board/tests/test_board_replay.sh
workflows/scripts/board/tests/test_boards_conf.sh
workflows/scripts/board/tests/test_milestone.sh
workflows/scripts/board/tests/test_parent_issue.sh
workflows/scripts/board/tests/fixtures/fake_gh.sh
workflows/scripts/board/tests/fixtures/project_view.json
workflows/scripts/build/tests/test_funnel_drive.sh
workflows/scripts/build/tests/test_funnel_tick.sh
workflows/scripts/build/tests/test_gate.sh
workflows/scripts/build/tests/test_pr.sh
workflows/scripts/build/tests/test_ci_poll.sh
workflows/scripts/build/tests/test_rework_snapshot.sh
workflows/scripts/build/tests/test_board_mirror.sh
workflows/scripts/build/tests/test_archive_plan.sh
workflows/scripts/build/tests/test_workflow.sh
scripts/tests/test_stranger_config.sh

# The CLI's newcomer-facing install doc (foundation #765 Epic D, item
# cli-entrypoint-bootstrap). Its ONLY denylisted content is the kernel repo's
# own public clone/raw URL inside the copy-paste curl|sh code fences — the
# repo's identity, the same category-1 "this repo's own real value" rationale
# as bootstrap.sh's per-line markers (and board.sh's board-7 entry). The
# per-line marker can't be used here without shipping lint jargon to readers:
# sources/cli.py renders this file VERBATIM into the public getting-started
# docs page, so an in-fence trailing `# denylist:allow` comment would appear
# inside the newcomer's first copy-paste command.
bin/README.md

# The kernel's own top-level public front page (foundation #894,
# readme-quickstart). Same exact rationale as bin/README.md immediately
# above: its § 3 quickstart repeats bin/README.md's install curl|sh fences
# verbatim (so a reader following ONLY the top-level README, never scrolling
# into bin/, still gets a copy-paste-able install step) — the kernel repo's
# own public clone/raw URL, not a leak, and a per-line marker would again
# land inside the newcomer's first copy-paste command.
README.md

# test_install_settings.sh deliberately feeds a literal personal path
# (/Users/travis/...) as INPUT fixture data to assert install-settings.sh's
# generic /Users/<user>-sed correctly strips it at install time (the test
# would be pointless with an already-generic input; temperloop#164/#169
# retired the CANONICAL_USER constant this comment used to name — the sed
# pattern is now username-generic, not tied to any one literal). Same
# rationale as the exemptions above: the literal is the test subject, not a
# leak.
workflows/scripts/tests/test_install_settings.sh

# The PR-leak-guard's OWN fixture test (temperloop #74). To prove the guard
# catches a real-pattern leak it must MENTION the very tokens it detects
# (/Users/travis, travnew@yahoo.com, Towheads) — in its seeded fixtures, its
# "# matches ..." comments, and its output assertions. Those literals are the
# test subject, not a leak — identical rationale to test_install_settings.sh
# immediately above. Exempting the file wholesale keeps both this whole-tree
# check AND the diff-scoped guard (which shares this exempt list) green when
# they scan the fixture. gitleaks does not read this list, but the file carries
# no secret literal, so the whole-tree gitleaks scan stays green regardless.
workflows/scripts/kernel/tests/test_check_pr_leak_guard.sh

# Synthetic cwd fixtures for tidy's session-stub parsing tests; the
# personal-looking path is deliberately realistic test input, never a real
# session.
workflows/scripts/drain/tests/fixtures/sample_stub.md
workflows/scripts/drain/tests/fixtures/command_expansion_stub.md

# The denylist tooling's OWN data files. personal-token-denylist.tsv's job is
# to CONTAIN the pattern strings (that's the data, not a leak), the burn-down
# baseline's job is to QUOTE each pre-existing offending line verbatim so the
# checker can match it exactly (temperloop#164/#169 — the debt ledger IS the
# quoted offenders), and this exempt-files list's own comments cite example
# paths/patterns for the same documentary reason — all three self-match every
# time they're scanned otherwise (and the baseline's added lines would trip
# the diff-scoped PR leak guard, which shares this list, on the very PR that
# introduces them).
workflows/scripts/kernel/personal-token-denylist.tsv
workflows/scripts/kernel/personal-token-denylist-baseline.tsv
workflows/scripts/kernel/personal-token-denylist-exempt-files.txt
