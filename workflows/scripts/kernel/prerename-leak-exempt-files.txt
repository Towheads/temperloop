# prerename-leak-exempt-files.txt — files WHOLESALE exempted from
# check-prerename-leak-guard.sh (temperloop#433), one git-relative path per
# line. `#` starts a comment; blank lines ignored.
#
# Deliberately NOT a reuse of personal-token-denylist-exempt-files.txt (the
# sibling gate's own exempt list): that list wholesale-exempts bin/README.md
# and README.md for an UNRELATED reason (they repeat the kernel's own public
# clone/raw URL in a copy-paste code fence), and this gate's acceptance
# criteria explicitly require it to scan bin/README.md (the bootstrap
# hand-synced triplication doc) — so blanket-including that file here would
# reintroduce exactly the blind spot this gate exists to close. The three
# board/build fixture-replay tests below ARE also on that other list, for a
# genuinely different (personal-org-token) reason — they're
# re-listed here explicitly rather than shared via an include.

# Three board/build fixture-replay tests whose mock `gh` JSON/CLI data embeds
# full GitHub URLs for the real, still-existing `<org>/foundation` build
# repo (board 4) — e.g. ".../repos/<org>/foundation/issues/145" — which
# incidentally contains the substring `foundation/issues`, matching this
# gate's `foundation/<leaf>` scan even though it has nothing to do with the
# temperloop kernel's own pre-rename identity. Same fixture data, same
# rationale, as their entries in personal-token-denylist-exempt-files.txt.
workflows/scripts/board/tests/test_parent_issue.sh
workflows/scripts/build/tests/test_pr.sh
workflows/scripts/build/tests/test_board_mirror.sh

# This gate's OWN fixture-replay test (temperloop#433). To prove detection
# it must seed a genuinely un-vetted FOUNDATION_-shaped token (one that is
# deliberately NOT in prerename-leak-verdicts.tsv) and assert the gate goes
# red on it, then removes it. That literal is the test subject, not a leak —
# identical rationale to check-pr-leak-guard.sh's own fixture test being
# exempted from check-personal-token-denylist.sh.
workflows/scripts/kernel/tests/test_check_prerename_leak_guard.sh
