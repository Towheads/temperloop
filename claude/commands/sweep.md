---
description: Drain a board's Ready **singletons** — issues triage left ungrouped (neither a sub-issue of an epic nor an epic parent) — in chunked parallel fanout. Phase 1 surfaces every blocking clarifying question in one batch and records the answers; Phase 2 fixes the pool in **chunks of `SWEEP_FANOUT_WIDTH` concurrent issues**, each chunk one multi-item `build-level.mjs` invocation (claim → isolated workers in parallel → PR → CI) followed by a per-chunk merge pass, parking any issue whose worker hits a question and moving on (`SWEEP_FANOUT_WIDTH=1` = the legacy one-at-a-time behavior). On an **attended** run at width > 1, the **overlap tier (tier 2)** launches the clean first chunk as a background invocation *before* the Phase-1 question batch and drives answered issues in a same-run **tail chunk**. The singleton-path peer to `/build` (which drains the epic'd work).
argument-hint: "[--board <N>] [--dry-run] [--unattended]"
---

You are running the **sweep** command. Goal: drive every **Ready singleton** on a board — an issue triage marked Ready but left **ungrouped** (it is *neither* a sub-issue of an epic *nor* an epic parent) — from open to merged, in **chunks of up to `$SWEEP_FANOUT_WIDTH` concurrent issues**. This is the singleton-path peer to `/build`: `/build` drains the *epic'd* work (via a plan note); `sweep` drains the *lone* Ready issues. Together they clear the whole Ready pool with no overlap — the "neither a sub-issue of an epic nor an epic parent" filter is the seam between them.

```
                 ┌─ epic'd survivors ─► /assess --epic N ─► /build
/triage  Ready ──┤
                 └─ singletons       ─────────────────────────► /sweep   ◄── you are here
```

Because the singletons skip `/assess`, they have **no pre-execution contract-clarification stage**. So `sweep` adds one: **Phase 1**, an upfront question sweep (the singleton path's missing `/assess`-equivalent) that resolves ambiguity *before* any fix worker runs.

## Inputs

- `--board <N>` (optional) — which board's singletons to drain. stageFind = `3` (repo `<org>/stageFind`), foundation = `4` (`<org>/foundation`). Explicit `--board` is **preferred**; if omitted, the board is **inferred from the local repo** (see Step 0.2), consistent with `/triage` and `/assess`. Inference is bounded to the repo you're standing in — it resolves only to that repo's registered board and prints the resolution before acting, so it cannot silently pick an *unintended* board (the prior arbitrary stageFind-`3` default is gone; foundation#547 established that bounded, printed inference is not the silent-board footgun the old default was).
- `--dry-run` (optional) — rehearsal: validate + build the pool + run Phase 1's detection, then **print** the pool + which issues carry open questions, with **zero** mutation (no claim, no worker, no merge, no label/comment writes). See Step 3's dry-run note.
- `--unattended` (optional) — **default posture** (per the batch-unattended convention): after Phase 1's answers are in, Phase 2 runs without further prompts — it auto-merges each green PR at the per-chunk merge pass and **parks** anything that escalates rather than halting. Phase 1 is the one interactive gate; on a truly operator-absent run it leaves all flagged issues parked and drives the clean (un-flagged) set — now chunked, like every run mode (see Step 2). An unattended/headless run **never** uses the tier-2 overlap (#626 — a headless run has no background-completion re-invoke loop): it keeps the tier-1 synchronous chunked path of Step 3 exactly as specified, with no background launch of any kind.

## Operating principles

- **Reuse `build-level.mjs` — do not reimplement the fix loop.** The per-issue mechanics (claim → worktree → isolated worker → acceptance gate → scan → push-by-SHA → PR open → CI poll) already live in the saved Workflow `claude/workflows/build-level.mjs` (foundation #422), which drives a *set* of items and returns a small `{parked, escalations}`. `sweep` invokes it **once per chunk** — a multi-item "level" of up to `$SWEEP_FANOUT_WIDTH` issues, executed through the workflow's own within-level `parallel()` path (the exact machinery `/build` uses daily) — chunk after chunk. No new workflow file; the command is a thin conversational driver over the existing workflow + machinery. See [[Decisions/foundation - build workflow-machinery invocation seam]]. <!-- cite: SW.1 guard:claude/workflows/build-level.mjs -->
- **Per-chunk memory isolation is structural, not aspirational.** Each per-chunk workflow invocation is a **fresh process**, and every fix worker inside it is an isolated `agent({schema})` whose context is **discarded on return** — so nothing bleeds between issues or between chunks, and the conversational orchestrator only ever sees the small `{parked, escalations}` object per chunk (bounded context across the whole sweep). <!-- cite: SW.2 guard:workflows/scripts/build/build.config.sh -->
- **Chunked fanout — parallel within a chunk, synchronous between chunks.** The Phase-2 set drives as **chunked multi-item invocations**: consecutive groups of up to `$SWEEP_FANOUT_WIDTH` issues form one `build-level.mjs` invocation (a multi-item `items[]` array), and each invocation runs **synchronously to completion** before the next chunk launches. Merges batch at chunk boundaries (the per-chunk merge pass), the quota gate runs between chunks, and board WIP rises to at most the chunk width — the same accepted properties `/build` has per level. **`SWEEP_FANOUT_WIDTH=1` is the rollback lever: it restores full legacy behavior — strictly one-issue-at-a-time drive *and* questions-first ordering — as a config-only change, no spec revert needed.** (On every tier-1 arm — width 1, unattended/headless, degraded, or `--dry-run` — Phase 1 always completes before any Phase-2 chunk launches, so questions-first holds; the `=1` guarantee is what keeps that pinned now that tier 2, next bullet, relaxes ordering at width > 1 on attended runs.) The ratified design's **tier 2** — the attended question-overlap tier — is **now part of this spec**: the next bullet defines it; Step 2 owns its launch and the chunked fix loop its composition (`docs/adr/0012-sweep-two-tier-parallel-execution.md`).
- **Tier 2 — attended question-overlap: chunk 1 in the background, a same-run tail chunk.** On an **attended** run, once Phase-1 detection has partitioned the pool into a **clean set** (no open question) and a **flagged set**, the orchestrator launches **chunk 1** — the clean set's *first* chunk, up to `$SWEEP_FANOUT_WIDTH` issues (the width cap holds for it like any chunk) — as a **background Workflow invocation *before* presenting the Phase-1 question batch**: the call returns immediately with a task id, and the operator answers the batch while chunk 1 builds. Issues whose questions the operator answers during the batch accumulate into the **tail chunk** — *definition:* the chunk assembled from the question batch's **answered** issues, driven **in this same run** after the batch resolves, sequenced after any chunk whose merge pass is still pending, and honoring the per-chunk quota gate exactly like every other chunk (an unanswered/deferred issue never joins it — it stays flagged for the next run; answered issues beyond `$SWEEP_FANOUT_WIDTH` split into further width-capped tail chunks). Tier 2 activates **only** when all four hold: **(a)** the run is **attended** (a live operator — never the `--unattended`/headless arm, #626: a headless run has no background-completion re-invoke loop, so the unattended arm keeps the tier-1 synchronous chunked path unchanged); **(b)** `$SWEEP_FANOUT_WIDTH` > 1 (width 1 = full legacy semantics — sequential drive *and* questions-first ordering — tier 2 disabled); **(c)** background Workflow invocation is **available** in the harness; **(d)** the run is **not `--dry-run`** (the rehearsal's zero-mutation guarantee forbids any background launch — the chunked fix loop's dry-run note prints what chunk 1 *would* have been instead). When (c) fails at run time — the background launch is unavailable or refused — emit the **degradation notice** in Step 2's tier-2 block and fall back to the tier-1 synchronous path: never a silent behavior change, never a stall (synchronous fanout is the designed floor, not an improvisation). *Mechanism note (for executors):* the harness **Workflow-tool contract** is the mechanism — a background invocation returns immediately with a **task id**, and the harness delivers a **`<task-notification>` that re-invokes this conversational driver on completion**, where the chunk's merge pass then runs (the chunked fix loop's tier-2 composition). This is **distinct** from `claude/commands/build.md`'s worker-prompt rule "No long-running background work … a backgrounded gate/eval silently dies": that rule concerns a *subagent's* backgrounded shell process inside a headless worker — reaped when the worker yields, with no notification path back to it — not the conversational driver's own Workflow call, which *does* receive the completion notification and resumes. The pre-report terminal-state assertion remains the backstop against a dropped background chunk: every Phase-2 issue must reach exactly one terminal disposition **regardless of which tier drove it**. <!-- cite: SW.3 incident:F#626 -->
- **Shared-hotspot / composition-root awareness.** When consecutive singletons touch the same file — the canonical example is a composition root (e.g. `AppComposition.swift` / `SettingsView.swift` in a mobile app), but any file that registers/wires up components plays the same role — N's merge can land while N+1 is mid-build, leaving N+1's worktree on a stale base. The pre-PR-open rebase (the `pr.sh rebase` step at `/build`'s 3f push/PR-open boundary, #525) surfaces this conflict deterministically as a `rebase-conflict` escalation rather than a post-PR-open CONFLICTING dead-poll. When partitioning the Phase-2 set into chunks (Step 3), **sequence hotspot-touching singletons deliberately — never into the same chunk**: if two or more Ready issues are known to touch the same shared file, keep the highest-priority one in its natural chunk and defer the others into a **later chunk** — a deferred sibling then rebases cleanly onto the earlier one's already-merged result at its own chunk's pre-PR-open rebase. Two same-file siblings that do end up concurrent surface as a deterministic `rebase-conflict` park, never a corrupt merge — the heuristic reduces parks; the rebase is the correctness backstop. **`CHANGELOG.md` is deliberately NOT one of these hotspots — that is a resolved concern, not a missing case.** It used to be the one file *every* contract-surface kernel item touched, so a multi-item kernel chunk collided on it by construction rather than by coincidence (temperloop#1218). The fix was made at the source rather than in this heuristic: a contract-surface PR now writes its own new `changelog.d/<slug>.<category>.md` fragment — a distinct new path — and `CHANGELOG.md` is rewritten only at the release cut (temperloop#1321 added the fragment directory and its assembler; temperloop#1322 cut the gate over to *requiring* a fragment). Two singletons in one chunk therefore share no changelog line and cannot collide on one, so the chunker needs no CHANGELOG-specific rule and re-adding one would be dead weight. The release cut *does* rewrite `CHANGELOG.md`, but `VERSIONING.md` mandates the cut be one deliberate maintainer PR and `/sweep` only ever drives the Ready singleton pool — a cut never enters a chunk. <!-- cite: SW.5 incident:F#525 -->
- **Park-on-question, no modal halt.** When an issue's worker returns a question (a `blocked` / `design-fork` / `failed` escalation), `sweep` does **not** halt the run or open a conversational round-trip (the `/build` 3d-esc behavior). It **parks** the issue on the board — moves it back to `Ready` (out of In Progress), adds the `needs-clarification` label + the question as a comment, and **assigns the operator** (routing it into their assigned-to-me queue at source, foundation #684) — and **advances**, disposing the rest of the chunk and then the remaining chunks. The `needs-clarification` **label** is the open-question gate (there is no `Blocked` Status bucket — it was retired in #435; an open *question* is a label, just as a dependency block is a native `blocked_by` edge). Parked issues re-enter the **next run's Phase 1** automatically — the label, not a status, carries the question across runs, so the loop self-heals. <!-- cite: SW.4 incident:F#684 -->
- **Claim-first, per chunk — a multi-claim window, claims held until Done.** The board is a cross-session lock. Each of a chunk's issues is **claimed In Progress as the first per-item action** (the workflow's own claim-first step, before its worker investigates), so a driving chunk holds up to `$SWEEP_FANOUT_WIDTH` claims at once — **WIP = the chunk width**, and a chunk is exactly the kernel's documented **multi-claim window** (K#275). Claims are **held until Done**: an issue's board half leaves In Progress on merge/close — no cascade automation exists on this backend, but the closed state alone makes the item read Done immediately (`issue_item` checks `state == closed` before any label) — or via the park path's explicit status move, never a mid-flight release. Consequently `release.sh`'s one-marker-per-window design means it correctly **refuses** to clear a non-latest claim marker — that refusal is **expected and non-fatal** (the marker clear is best-effort; see the Phase-2 park path). Phase 1's read pass is shallow triage-style detection (like `/triage` reading Backlog or `/next` reading Ready) and does not claim — only Phase 2's chunk loop claims.
- **The conversational/workflow split (mirrors `/build`'s default Workflow path).** The **workflow** owns each chunk's 3a–3g and returns `{parked, escalations}` — it never merges, never writes outside its return. **This command (conversational)** owns: Phase 1 (`AskUserQuestion` cannot run inside a workflow), the **per-chunk merge pass**, the **park-on-question** board writes, and the report.
- **The `needs-clarification` label is the open-question marker.** `/triage` drops it (with a `needs-clarification: <question>` comment) on underspecified survivors at source; `sweep` Phase 1 consumes and clears it; park-on-question re-adds it. It is also consumed by `/assess` on the epic path, and — on an autonomous board — **drained by the pipeline** when the operator answers + unassigns (the label is cleared so the item drives again; foundation #657). A plain GitHub label (advisory, mirrors `spike`).
- **Board-native only.** This command requires the board adapter + a registered board (the Step 0 probe). With no board it stops — there is no boardless mode (the singleton pool *is* a board query).
- **Live candidate tagging provenance (temperloop#1257, epic #1225 "model comparison harness") adds no new selection mechanism.** An operator points the existing `$SWEEP_WORKER_MODEL` setting (Step 0.4) at a candidate exactly as they would to change the default tier — that is the *only* lever, and `workflows/scripts/model-comparison/tagging.sh` has no `--model` flag of its own to disagree with it. What live tagging adds is provenance: `tag` (Step 3's per-chunk merge pass, `pr` is set) records a bounded, run-scoped window entry, emits a matching attribution-only telemetry tag through the existing `emit-model-usage.sh` raw lake (temperloop#1253/#1255 — reused, never a parallel stream), and stamps the merging PR's body with model+provider (never a key, never content) **before** it merges. `tagging.sh crosscheck` is the mechanical proof a reviewer or auditor can run later: it fails loudly if a PR's stamp and its recorded provenance ever disagree. Designation is governed by the same committed provider allowlist (`workflows/scripts/model-comparison/allowlist.sh`) every other provider check in this module reads — an unlisted provider is refused, silently skipping the stamp/tag rather than blocking the merge (see Step 3). This is **distinct** from Step 3.6's `emit-command-run.sh` record (`--command sweep`, the whole-run tally) — the two streams answer different questions (which model produced *this PR* vs. how many issues *this run* processed) and both fire on a live-tagged run with no conflict.

## Step 0 — Validate + board probe

Reuse `/build` Step 0's probe verbatim. Run in parallel:

1. `gh auth status` — must succeed (board reads/writes and the per-chunk PRs need an authenticated `gh`). The default **`repo`** scope is sufficient; **never check for or require a `project` scope** — every board read/write is a plain-REST label/issue/sub-issue call (`workflows/scripts/board/ISSUES-ONLY-BACKEND.md`), so a token carrying only `repo` drives the whole command. Unauthenticated → stop with the `gh auth login` hint.
2. **Board probe.** Set `BOARD_LIB` = the first of `scripts/lib/board.sh` or `workflows/scripts/board/lib/board.sh` that exists; `source "$BOARD_LIB"`. Resolve the board from `--board`; **if omitted, infer it from the local repo** (the same reverse-lookup `/triage` Step 0.3 uses): `repo=$(gh repo view --json nameWithOwner -q .nameWithOwner); BOARD=""; for b in $(board_registered_boards); do [ "$(board_repo "$b")" = "$repo" ] && BOARD="$b"; done` — the candidate list is the adapter's own `board_registered_boards` (the single source of truth; board **7** is the temperloop kernel tracker, issues-only — a hardcoded `3 4 5 6` here is the temperloop#352 drift that dropped it). If a match is found, print `inferred board $BOARD (repo $repo)` before any board read; if **no** candidate matches (an unmapped repo), STOP with `/sweep: cannot infer board — pass --board <N> (3=stageFind, 4=foundation, 5=ssmobile, 6=subsetwiki, 7=temperloop kernel) or run from a board-mapped repo`. Set `CLAIM`/`RELEASE` = the `claim.sh`/`release.sh` next to `BOARD_LIB` (or the `claim`/`release` commands on `PATH`). Confirm the repo maps to the board: `repo="$(board_repo "$BOARD")"`. **No adapter or no registered board → stop** ("board integration unavailable — run from a board-enabled checkout").
3. **Resolve the workflow-invocation context** (passed to every per-chunk `build-level.mjs` call): `repoRoot="$(git rev-parse --show-toplevel)"`, `ownerRepo="$(gh repo view --json nameWithOwner -q .nameWithOwner)"` (the workflow has no shell to derive it), and `claimCmd` = the absolute path to `claim.sh` (`$(dirname "$BOARD_LIB")/../claim.sh`, or `claim.sh` on PATH). Also resolve `workflowPath` = the deployed Workflow script (`"$HOME/.claude/workflows/build-level.mjs"`) — Phase 2 invokes it by **`scriptPath`**, NOT `name:` (the Workflow tool's `name:` resolves built-ins ONLY — deep-research/code-review — never deployed user workflows; #437). And resolve **`machineryBinDir`** — the already-resolved absolute path to the build-machinery script directory (`worktree.sh`/`pr.sh`/`ci-poll.sh`) — with the **same plain `cd`+`pwd` form `/build` uses** (`claude/commands/build.md` Step 3's `machineryBinDir` bullet is the single statement site; this is a pointer, not a second spec): `machineryBinDir="$(cd workflows/scripts/build 2>/dev/null && pwd)"` — a plain `cd`+`pwd` that follows a `workflows/` → foundation symlink to the real path. Passing it lets `build-level.mjs` emit **plain absolute machinery paths** instead of resolving them inline with a nested `$(dirname "$(readlink -f …)")` command-substitution, which the auto-mode safety classifier read as an obfuscated-command bypass and **denied** on `--unattended` runs (temperloop#72) — and `--unattended` is `/sweep`'s **default** posture, so the un-passed shape produced recurring machinery-denied escalation bursts on every push/worktree step. Absent → the `.mjs` falls back to its in-bash `machineryBin` resolver, so this is a classifier de-trip, **not** a hard dependency.
4. **Source the batch-pipeline config + resolve the quota gate (best-effort).** `source workflows/scripts/build/build.config.sh` (bare repo-relative, the same convention this command already uses for `workflows/scripts/build/worktree.sh` in the park-cleanup step) — pulls the `BUILD_QUOTA_*` settings into scope for the Phase-2 quota gate, plus the sweep settings: **`SWEEP_FANOUT_WIDTH`** (the Phase-2 chunk width; every later reference in this spec is symbolic — on a non-vendoring checkout where the config file is absent, use the belt-and-suspenders `${SWEEP_FANOUT_WIDTH:-3}` form and the config file's own header stays the single home of the default), **`SWEEP_DETECT_MODEL`** (the Phase-1 detection-fanout model tier; empty = inherit the session model), **`SWEEP_WORKER_MODEL`** (the Phase-2 fix-issue WORKER model tier — distinct from `SWEEP_DETECT_MODEL`; empty = inherit the session model, same sentinel convention), **`BUILD_MACHINERY_SOLO_MODEL`** / **`BUILD_MACHINERY_BATCH_MODEL`** (the `build-level.mjs` machinery-executor model tiers this command's own Phase-2 Workflow invocation also passes through — same paired setting `/build` uses, empty = unchanged), **`BUILD_GATE_SLICE_SECS`** (the parent-side acceptance gate's per-SLICE budget, passed through the same hand-off as `gateSliceSecs`; belt-and-suspenders form `${BUILD_GATE_SLICE_SECS:-300}`), **`BUILD_MACHINERY_STEP_CEILING_SECS`** / **`BUILD_MACHINERY_STEP_SLOW_SECS`** (the `build-level.mjs` machinery-step wall-clock liveness ceiling and its progress-notice threshold — temperloop#1071 — passed through the same hand-off as `machineryStepCeilingSecs` / `machineryStepSlowSecs`; belt-and-suspenders forms `${BUILD_MACHINERY_STEP_CEILING_SECS:-900}` / `${BUILD_MACHINERY_STEP_SLOW_SECS:-300}`), and **`SWEEP_BG_POLL_ATTEMPTS`** / **`SWEEP_BG_POLL_INTERVAL`** (the tier-2 bounded wait on background chunk 1's completion notification — the chunked fix loop's tier-2 composition; same belt-and-suspenders form applies: `${SWEEP_BG_POLL_ATTEMPTS:-6}` / `${SWEEP_BG_POLL_INTERVAL:-120}`). Then resolve and **guard** the gate script: `quotaGate="workflows/scripts/build/quota-gate.sh"; [ -x "$quotaGate" ] || quotaGate=""`. If the config/script is absent (a non-vendoring checkout), proceed: an empty `$quotaGate` makes the Phase-2 quota gate a no-op (the optional gate must never break or stall a run).
5. **Resolve the effective engineering principle set (once per run) — the `principlesSummaries` / `principlesDefaultRepo` hand-off.** Runs **after** item 3 (it needs `ownerRepo`), and is **orchestrator work, not `.mjs` work**: the Workflow runtime has no filesystem or vault access to merge `claude/engineering-principles.md` with a project's own `## Principles` extension itself (`build-level.mjs`'s DESIGN NOTE 1 — the same wall `machinerySoloModel`/`gateSliceSecs` hit at Step 0.4), so this live session is the only place the merge can happen. **Run `/build` § Step 1.8's procedure verbatim — `claude/commands/build.md` is the single implementation and this is a pointer to it, so the two can never drift.** Only its *pair enumeration* simplifies here: a sweep drains **one** board, so there is exactly **one** `(repo, project)` pair — `repo = ownerRepo`, `project = basename(ownerRepo)` — and no cross-repo plan item to add a second. Render that pair per Step 1.8 items 2–4, then set `principlesSummaries = { "<ownerRepo>": "<that pair's rendered summary>" }` and `principlesDefaultRepo = ownerRepo`, and pass **both** in every chunk's Workflow args (Step 3). Without this hand-off every `/sweep` worker permanently runs the `.mjs`'s static kernel-only `DEGRADED` fallback. **Degrade, never halt** (principles are advisory — `claude/engineering-principles.md` § Advisory posture): if the kernel file or the vault read is unavailable, resolve what you can and, if nothing resolves, omit the two keys and let the `.mjs`'s own `DEGRADED` notice stand — never stall a sweep over it.

If any check fails, surface in one line and stop.

## Step 1 — Build the singleton pool

`board_resolve "$BOARD"` once (whole-board read). From `BOARD_ITEMS_JSON`:

1. **Fix pool** = items whose `.status` is `Ready` **AND** that are **neither a sub-issue of an epic nor an epic parent** — for each Ready item, both `board_parent_issue "$BOARD" <issue#>` (the REST-cheap parent check, the same one `/next` uses) **AND** `board_sub_issues "$BOARD" <issue#>` (the read-side counterpart — same REST bucket, foundation #800) print **empty**. A Ready item *with* a parent is an epic leg → it belongs to `/build`, **skip it**. A Ready item *with* native sub-issues is an epic parent itself — its children are the real work items → **skip it** too, exactly as a sub-issue is skipped.
2. **Exclude `funnel-escalated` items** — a Ready singleton carrying the `funnel-escalated` label (foundation #697) is a stuck code item the autonomous pipeline's level-5c merge tier could not land (route-refused / terminally-red CI): it already has an **open or failed PR** and is assigned to the operator, awaiting a **manual merge or close**. `sweep` has no resume-mode (unlike the pipeline's #624 hand-off), so driving it through Phase 2 would open a **duplicate PR**. **Drop it from the fix pool** the same way an epic leg is skipped, and **report it** in Step 4 as escalated/skipped (it clears when the operator closes the issue or removes the label — closing/merging the PR alone does not remove an issue label). This is the read-time counterpart of the `pipeline_escalated` drive-park gate in `pipeline-tick.sh` and the `/next` skip — every Ready-pool consumer that *drives* must honor it. (Distinct from `needs-clarification`, which is an answerable question Phase 1 consumes, not an open-PR escalation.) <!-- cite: SW.6 incident:F#697 -->
3. **Parked-on-question items re-enter automatically** — a prior run's parked issue stays in `Ready` carrying the `needs-clarification` label, so it is **already in the fix pool above**; Phase 1 (Step 2) re-detects the label and re-surfaces its question. There is no separate `Blocked` re-entry pool — the retired `Blocked` bucket (#435) is gone; the label, not a status, is what carries the open question across runs.

`board_parent_issue` and `board_sub_issues` are both per-item REST calls — gate **both** on the `Ready` slice only, never the whole board (no whole-board REST fan-out). Pull each pooled issue's title, body, labels, **and comments** with `gh issue view <n> -R "$repo" --json title,body,labels,comments,url` — the comments feed Step 2's answered-exclusion check (a `Clarified (…)` comment can already sit on an issue whose `needs-clarification` label or open fork was never cleared).

If the pool is empty, say so in one line and stop.

## Step 2 — Phase 1: the upfront question sweep (the one interactive gate)

A pooled issue **has an open question** iff it **carries `needs-clarification`** (triage-flagged or parked by a prior run) **OR** your own read of it judges it **underspecified** — the fix can't start because the intended behavior, the choice between two designs, or a missing decision is genuinely ambiguous (not merely a fact a worker could look up). When *you* judge an unflagged issue underspecified, **comment the question FIRST, then label+assign** (`gh issue comment <n> -R "$repo" --body "needs-clarification: <the question>"$'\n\n'"Once answered: on the autonomous pipeline board, unassign yourself to release it straight back to the driver; on other boards leave it as-is — the next /sweep or /assess clears the label. Either way your answer is consumed."`, and only after it succeeds `gh issue edit <n> -R "$repo" --add-label needs-clarification --add-assignee @me`), so the question is durably captured **before** the "handled" markers land and the issue is routed to the operator's assigned-to-me queue at source like a triage flag (the pipeline router then merely parks it, never re-derives/re-posts the question the way the retired `route-needs-input` did — foundation #684; if the comment call fails, do not label/assign — leave it un-flagged to re-enter the next run). **Under `--dry-run` this detection is read-only** — identify and *print* which issues carry or raise questions, but make **no** comment/`--add-label`/`--add-assignee` write and **no** `AskUserQuestion` (the dry-run's zero-mutation guarantee — see Step 3).

**Answered, not underspecified — the comment-anchored exclusion.** Before either arm above raises a question for a pooled issue, check its comments (pulled by Step 1's fetch) against the issue's most recent **flagging-type** comment — either a `needs-clarification: <question>` comment (triage's, per triage.md, or a self-judged flag posted by this section) or a `Parked by sweep — <question>` comment (the Phase-2 escalation-park path, Step 3 below). If a `Clarified (…)` comment (e.g. `Clarified (sweep): <answer>`, `Clarified (triage): <answer>`) postdates that most recent flagging-type comment, the issue is **answered, not underspecified**: it carries no open question. This holds for **both** detection arms — the self-judged-underspecified arm and the already-`needs-clarification`-labelled carrier arm — so a stale surviving label alone does not re-raise an already-answered question, and your own read must not independently re-flag it either. Exclude it from the question batch below and from the detection fanout's `underspecified?` verdict.

**Run the underspecification detection as a parallel subagent fanout.** Partition the pooled issues among concurrent **read-only** subagents dispatched in a single message (one slice per subagent; the already-flagged `needs-clarification` carriers need no re-judging for underspecification — they are question-carriers by definition, only their recorded question is gathered — but DO need the answered-exclusion check above applied, since their comments may already carry a later `Clarified (…)` answer). Each subagent reads its slice's title/body/labels **and comments** and returns, per issue, an `underspecified?` verdict plus the derived question when yes — applying the answered-exclusion rule above, so an issue carrying a later `Clarified (…)` comment returns `underspecified? = no` regardless of an open fork elsewhere in its body — findings only, no board or issue writes (all Phase-1 writes stay in this conversational layer, and under `--dry-run` even those are suppressed). Set each detection subagent's model **explicitly** to `$SWEEP_DETECT_MODEL`; its **empty default means inherit the session's own model** — a deliberate exception to cheap-tier fanout defaults, because ambiguity detection is judgment work and a missed ambiguity that silently reaches Phase 2 is the costly failure mode (§ Cost-tier routing, and the setting's own header in `workflows/scripts/build/build.config.sh`). **Detection-slice failure mode:** a slice whose subagent errors, times out, or returns malformed/partial output has produced no usable verdict — **retry that slice once**; if the retry also fails, treat **every issue in the slice as `underspecified`** (fail-safe toward asking, never toward silent pass-through) and surface each in the Phase-1 batch with a note that the detection pass could not judge it. A failed slice's issues must never proceed to Phase 2 as if judged clear. <!-- cite: SW.7 class:unjudged-ambiguity-pass-through -->

**Tier-2 overlap launch (attended, `$SWEEP_FANOUT_WIDTH` > 1 only).** When the tier-2 activation conditions hold (Operating principles: attended + width > 1 + background invocation available + not `--dry-run`), launch **chunk 1 here** — after detection has partitioned the pool into the clean and flagged sets, **before** surfacing the question batch below:

1. **Mint the Step-3 TaskCreate checklist for the clean set FIRST** (one `[ ] #N: <title>` entry per clean issue, pool order) — the worklist-lock must exist before any chunk, background included, launches; the tail chunk's entries are appended as answers land (Step 3).
2. Partition the **clean set** into chunks per Step 3's rule (pool order, hotspot deferrals applied), and launch the **first** chunk as a **background** Workflow invocation — the identical `build-level.mjs` invocation shape as Step 3's, run in the background instead of foreground. The call **returns immediately with a task id**; record it against the chunk. The remaining clean chunks are **not** pre-spawned — they drive synchronously in Step 3.
3. Proceed to the question batch with chunk 1 building concurrently. Its completion **`<task-notification>` re-invokes this driver**; chunk 1's merge pass runs at Step 3's tier-2 composition point (never while an `AskUserQuestion` is open).

**If the background invocation is unavailable or refused at launch time**, do not stall and do not silently change shape — emit this degradation notice (the message-schema **Degradation notice**, mode-2 form) and fall back to the tier-1 synchronous path (Phase 1 completes first, every chunk drives foreground in Step 3):

> skipped — tier-2 overlap launch (background Workflow invocation unavailable/refused). Falling back to the tier-1 synchronous path: the question batch completes first, then every chunk drives in the foreground. Results and coverage are unaffected — only the wall-clock overlap is lost; every issue still reaches a terminal disposition through the same chunk loop.

On the `--unattended` arm this block is skipped entirely (tier 2 never runs headless, #626), and **under `--dry-run` there is no background launch of any kind** — Step 3's dry-run note prints what chunk 1 *would* have been.

1. **Gather every open question across the pool**, reading triage's recorded `needs-clarification: <question>` comment when present (else the detection fanout's derived question for the issue).
2. **Surface them in ONE batch** via `AskUserQuestion` (≤4 per call — loop in groups for more; fall back to a written list the operator answers if there are many).
3. **On each answer:** record it as an issue comment (`gh issue comment <n> -R "$repo" --body "Clarified (sweep): <answer>"`) and **remove** the label (`gh issue edit <n> -R "$repo" --remove-label needs-clarification`). A parked issue already sits in `Ready` (the open question, not a status, parked it — #435), so there is no status to flip back; clearing the label is what lets Phase 2 drive it. The recorded answer rides into Phase 2 as the synthesized item's `notes`. Under tier 2 the answered issue also **joins the tail chunk** — append its `[ ] #N: <title>` entry to the checklist and drive it this same run (Step 3's tier-2 composition).
4. **Unanswered / deferred** (the operator skips one) → leave it flagged and **skip it this run** (it stays for the next).

**Unattended / operator-absent run:** do **not** block forever on an absent operator. Take the safe default — **leave every flagged issue parked (flagged, skipped)** — and proceed to Phase 2 with only the **un-flagged** issues. This is a **`ask-at-checkin`** deferral ([[Context/foundation - AskUserQuestion severity taxonomy]]), so it must NOT default *silently*: per `claude/CLAUDE.md § Unattended pending-decisions surface`, append one `### open` entry to the pending-decisions surface (`Pipeline/pending decisions.md` vs the legacy `Context/pipeline - pending decisions.md` — target pinned by the append-target resolution rule of the path fallback convention, `claude/commands/check-in.md`) (in the knowledge store) via `mcp__obsidian-builtin__vault_append` — **Decision:** *sweep left clarifying questions unanswered (issues #N, …)*; **Default taken:** *leave-all-flagged; proceed with un-flagged only*; **Disposition:** *auto-taken (operator-absent); `needs-clarification` labels retained as the operational record*; **Status:** *open* — so the next `check-in` surfaces them (the label is the operational record; this entry is the check-in-facing signal `check-in` actually reads). **This is the fifth `ask-at-checkin` writer site** (registered in `claude/CLAUDE.md § Unattended pending-decisions surface` and `tidy.md` Step 3). <!-- cite: SW.11 class:silent-unattended-default -->

After Phase 1, the **Phase-2 set** = the fix pool minus every still-flagged issue. Under tier 2 that same set is already partially in flight: the clean set's chunk 1 is building in the background, and the batch's answered issues form the tail chunk — Step 3's tier-2 composition sequences the remainder.

## Step 3 — Phase 2: the chunked fix loop

**Record the ordered Phase-2 set as an explicit tracked artifact before driving any chunk.** Use `TaskCreate` to mint a checklist — one entry per issue (`[ ] #N: <title>`) in pool order. (Ordering note — under tier 2 the tail-chunk entries are the deliberate exception: they append in **answer-arrival order** as the batch resolves them, so the pool-order invariant governs the clean-set entries while the tail rides at the end in the order its answers landed — the invariant is relaxed there by design, not silently violated.) This is the worklist-lock: every entry must reach a terminal disposition before the Step-4 report runs, so skipping is structurally impossible. (Under tier 2 the clean-set entries were already minted at the Step-2 launch, before chunk 1 went into the background, and each answered issue's entry was appended as its answer landed — verify the checklist now covers the whole Phase-2 set, tail chunk included, and append any entry still missing.)

**Partition the Phase-2 set into chunks.** Consecutive groups of up to `$SWEEP_FANOUT_WIDTH` issues, preserving pool order — after applying the shared-hotspot rule (Operating principles): known same-file issues never share a chunk; the lower-priority sibling(s) defer past the first into a later chunk. With `SWEEP_FANOUT_WIDTH=1` every chunk is a single issue and this loop degenerates to the legacy one-issue-at-a-time drive — the rollback lever. (Under tier 2 the clean set's first chunk is **already in flight** as background chunk 1 — partition only the *remaining* clean issues into subsequent chunks, and append the **tail chunk** last, itself width-capped and hotspot-checked like any chunk.)

For each chunk, in order, invoke the saved Workflow `build-level.mjs` **once** — a **multi-item level** carrying the whole chunk's `items[]` — via the Workflow tool:

```
// Invoke by scriptPath, NOT name: — the Workflow tool's name: resolves built-ins
// ONLY (deep-research/code-review), never deployed user workflows (#437). `args` is
// delivered to the script as a JSON STRING; build-level.mjs parses it itself.
Workflow({ scriptPath: workflowPath, args: {
  repoRoot, board: <BOARD>, ownerRepo, claimCmd, machineryBinDir,
                         // machineryBinDir resolved in Step 0.3 — a PLAIN absolute machinery path, so the
                         // .mjs never emits the nested $(dirname "$(readlink -f …)") substitution the
                         // auto-mode classifier denies on --unattended runs (temperloop#72, and
                         // --unattended is /sweep's default posture). Absent = the .mjs's in-bash
                         // machineryBin fallback, i.e. exactly that denied shape.
  principlesSummaries: <the Step 0.5 map, { "<ownerRepo>": "<rendered summary>" }>,
  principlesDefaultRepo: <$ownerRepo>,
                         // the effective (kernel ∪ project) engineering principle set, resolved ONCE per
                         // run in Step 0.5 on this same Step-0 hand-off seam (the Workflow runtime has no
                         // filesystem/vault to resolve it itself). Omitted → workerPrompt() falls back to
                         // its static kernel-only snapshot plus an explicit DEGRADED notice.
  machinerySoloModel: <$BUILD_MACHINERY_SOLO_MODEL>, machineryBatchModel: <$BUILD_MACHINERY_BATCH_MODEL>,
                         // both sourced in Step 0.4 — pass the resolved value straight through, empty or
                         // not (the .mjs's own `|| 'haiku'` fallback is empty-string-safe; temperloop#982)
  gateSliceSecs: <$BUILD_GATE_SLICE_SECS>,
                         // §3e.5 gate per-SLICE budget, same Step-0 hand-off seam and same empty-safe
                         // fallback (the .mjs clamps it under the agent Bash cap; temperloop#1021)
  machineryStepCeilingSecs: <$BUILD_MACHINERY_STEP_CEILING_SECS>,
  machineryStepSlowSecs: <$BUILD_MACHINERY_STEP_SLOW_SECS>,
                         // per-STEP wall-clock liveness ceiling + its progress-notice threshold, same
                         // Step-0 hand-off seam. The .mjs compiles the ceiling into the machinery
                         // command text itself (a sleep/kill watchdog INSIDE the invoked shell,
                         // independent of the Bash tool's own timeout, which temperloop#1071 observed
                         // failing to fire on a 9h49m step); a bounded-out step is treated as LOST and
                         // disposed through pr.sh recover-probe, never blind-retried. The .mjs clamps
                         // both and keeps its own defaults, so empty/absent is safe.
  planLink: "",          // EMPTY — singletons have no vault plan note; pr.sh wraps planLink in [[ ]]
                         // (a wikilink), so a URL here renders a broken link. Linkage rides
                         // `Closes #N` (from ghIssue) + `source: #N` instead. pr.sh skips the block on "".
  items: [ // ONE entry per issue in the chunk (up to $SWEEP_FANOUT_WIDTH of them) — the
           // multi-item array is what makes the chunk drive through the workflow's own
           // within-level parallel() path, exactly as a /build level does
  {
    slug,            // kebab from the title, SUFFIXED with `-<N>` (the issue number), total ≤40 chars —
                     // the number guarantees a unique worktree path `<repoRoot>.wt/<slug>` by construction
    branch,          // derived <type>/<slug> per [[Decisions/foundation - Branch naming convention]]
                     //   (type from the issue's labels: bug→fix, enhancement→feat, docs→docs, else fix)
    title,           // the issue title (refine to an imperative PR title)
    kind: <'spike' if the issue carries the spike label, else 'code'>,
    ghIssue: <N>,    // → the workflow's pr.sh open emits a bare `Closes #N`
    alsoCloses: [],
    model: <$SWEEP_WORKER_MODEL, sourced in Step 0.4 — pass it straight through, empty or not (the .mjs's
                     //   own `item.model || undefined` is empty-string-safe; empty = inherit the session
                     //   model, top tier, safe — today's unchanged default)>,
    acceptance: <checkable bullets from the issue body; if none, the Phase-1 answer, else "(self-verify the issue is resolved)">,
    source: "#<N>",
    scope: <the issue title / first body line>,
    notes: <the Phase-1 recorded answer, if any — so the worker sees the clarification>
  }, /* …one such entry per remaining issue in the chunk… */ ],
  verdicts: {}, onlySlugs: []
} })
```

The workflow claims each of the chunk's issues (3a — claim-first; the chunk is a **multi-claim window**, K#275), creates the deterministic worktrees (3b), runs the **isolated workers concurrently** (3c — the workflow's own within-level `parallel()`), runs the acceptance gate + closing-keyword scan + push-by-SHA + PR open + CI poll per item (3e.5–3g), and **returns one `{parked, escalations}` covering the whole chunk**. The invocation is **synchronous** — wait for it to return; nothing else is driven while a chunk runs.

**Per-chunk merge pass.** When the invocation returns, dispose **every issue in the chunk** — each appears in exactly one of `parked` / `escalations` — branching per issue:

**FIRST, before any merge in this pass: settle every host-config deferral parent-side (temperloop#1182).** `build-level.mjs` embeds its host-config deferral contract in **every** worker prompt, ungated — the worktree-is-populated-from-the-index fact holds on `/sweep` exactly as it does on `/build` — so a sweep worker can return an acceptance entry carrying `passed: false` **plus** `deferred_host_config`, and the shared `isHostConfigDeferral()` exclusion lets that item **park instead of escalate** here too. Such an item's parked record then carries **`host_config_deferrals: [{criterion, host_config}, …]`**, and those criteria are **UNVERIFIED, not passed**. `/sweep` has no §4a, so this is that seat: **you** are the actor that can settle them — this driver runs in `$repoRoot`, the real checkout, which *does* carry the gitignored file the worktree structurally could not. For each entry, before the `pr`-is-set arm below runs:

- **Run the criterion's own check in `$repoRoot`** — never the item's worktree, and **never** by copying the named `host_config` file into one (that is the secret-in-worktree exposure `/assess` A.8 exists to prevent, and it is not an option here at any urgency). And **never** by re-driving the issue: a re-drive lands in a worktree again and reproduces the identical uninformative reading. `/build` §4a owns the full disposition contract (which check counts, and the confirmed-set bar a bare presence probe does *not* meet) — follow it there rather than re-deriving it here.
- **Confirmed** → the criterion is met; proceed to the `pr`-is-set arm and merge exactly as normal. Note the parent-side confirmation (file/env var) in the report's per-issue detail.
- **Not confirmed** (genuinely unset on this host) **or cannot establish** (the check errored, or this checkout is not the host the criterion is about) → **do NOT merge.** The acceptance really is unmet or unknown, and an unattended sweep has no operator seat to widen it — so take the **existing escalation park** treatment below verbatim (its six steps: worktree remove → comment → label+assign → release → record), with the question naming the exact **file / env var the operator must populate** and which arm fired. Record the terminal disposition **parked**. This adds **no fourth disposition** — Step 3.5's three-way `merged` / `resolved (verdict)` / `parked` partition and the telemetry emitter's `merged + resolved + parked == items_processed` arithmetic close unchanged.

None of this relaxes the `/assess` A.8 confirmed-set bar; it moves **who** confirms, never **whether**. An item with no `host_config_deferrals` field skips this entirely — the common case, and a no-op on every ordinary sweep run.

- **`parked: [{ slug, pr, pushed_sha, acceptance_results, host_config_deferrals? }]`** (worker done) → **land it**, branching on whether a PR exists:
  - **`pr` is set** (a `code` item, CI green) → **first, the live-tagging provenance stamp — only when `$SWEEP_WORKER_MODEL` is non-empty** (a candidate is designated for this seat; sourced in Step 0.4, temperloop#1257): `stamp="$(workflows/scripts/model-comparison/tagging.sh tag --provider anthropic --run-id "pr:<pr>" --repo "$ownerRepo")"` — `anthropic` because `$SWEEP_WORKER_MODEL` only ever names a Claude-family model tier (a non-Anthropic candidate for this seat is a separate, later provider-selection lever, out of this item's scope; today's committed allowlist is Anthropic-only regardless). The run id is the PR number, doubling as the telemetry tag's `outcome_ref`. On success, append the printed stamp to the PR body **before** merging (`gh pr edit <pr> -R "$repo" --body "$(printf '%s\n\n%s' "$(gh pr view <pr> -R "$repo" --json body -q .body)" "$stamp")"`) — this is the whole point: a reviewer or a later auditor must never discover mid-review that a PR was candidate-authored. **This adds no new question surface and no new merge gate:** `tag`'s own refusal (an unset `SWEEP_WORKER_MODEL` — the no-op default — or a provider the committed allowlist doesn't currently allow) is a plain warn-and-skip here, never a park and never a halt; a tagging failure never blocks or delays the merge below. When `$SWEEP_WORKER_MODEL` is empty (the default, unchanged posture), skip this step entirely — nothing here fires on an ordinary sweep run. **Then merge** `gh pr merge <pr> --auto` (the queue owns strategy + branch lifecycle on a protected `main`). **No cache bust and no adapter Done write are placed here, deliberately** (temperloop#1164 / temperloop#1268): `--auto` only *enqueues*, so the issue is still **open** at this instant and its `Closes #N` fires server-side minutes later, when the queue actually merges. A `cache_dirty` here would therefore refetch and re-cache the issue as **open**, and the real close would still bust nothing — worse than no bust, because the path would *look* covered. Calling `board_close_done` here would be worse than a no-op: it would close the issue **before** the merge lands, breaking the `Closes #N` linkage (GitHub's closing-keyword match no longer has an open issue to act on) and the merge-queue accounting — exactly the misuse `board_close_done`'s own header warns against. Neither the bust nor the Done write belong here; both belong at a confirmed-`MERGED` point, which this arm does not have: `/sweep` records **fixed** at enqueue and never looks again (temperloop#1268). Wire both in as part of that issue, once this arm gains a merge confirmation to hang them on. Record **fixed** (with PR#). **Then converge the worktree in-lane:** `build-level.mjs` leaves the merged issue's worktree intact (the workflow removes no worktrees), so — exactly as the escalation branch below already does — reclaim it now: `workflows/scripts/build/worktree.sh remove "$repoRoot" <slug>` (the `$repoRoot` resolved in Step 0 — this session's OWN checkout, never a foreign canonical checkout, per the working-tree-ownership rule). This closes the **post-merge-convergence gap** so cleanup no longer relies solely on the next session-start sweep. It is **idempotent by construction, not by a new guard** — `worktree.sh` `cmd_remove` already returns `REMOVED` when a worktree or `build/<slug>` branch existed and `NOT_FOUND` when neither did, so a re-run is a safe no-op and there is no double-remove error. *(Backstop: the **deploy-mini session-start sweep** (F#653), which prunes merged locals in every clean-on-main checkout at session start, plus `worktree.sh prune "$repoRoot"`, remain the **crash-path** net for a session that died before this step — the safety net, not the primary mechanism.)*
  - **`pr` is `null`** (a **spike** singleton — the issue carried the `spike` label, so `build-level.mjs` parks a verdict-only item with `pr: null, pushed_sha: null` and **no PR**) → do **NOT** run `gh pr merge null` (a silent no-op that would falsely report "fixed"); **post the verdict comment, then land the Done write through the adapter**: `gh issue comment <N> -R "$repo" --body "Spike verdict captured (sweep)."`, then `if declare -F board_close_done >/dev/null 2>&1; then board_close_done "$BOARD" <N>; else gh issue close <N> -R "$repo"; fi` — the fallback guard covers a vendored `board.sh` that predates `board_close_done` (merged temperloop#1337; command specs are one machine-global symlink while `BOARD_LIB` resolves per-repo, so a stale checkout's `board.sh` can lack the helper), the same reasoning as the equivalent guard on the `/triage` call site. **Write it as `if`/`else`, NEVER the one-line `A && B || C` chain** — with `&&`/`||` the bare-close fallback fires whenever `board_close_done` *itself fails*, not only when it is absent, and that bare close strips **neither** the `fnd:status:*` label **nor** the `fnd:host/session:*` stamp; a transient adapter failure would then silently strand exactly the residue this call exists to remove, and exit 0 while doing it (strictly worse than having no helper, because the failure becomes invisible). A non-zero return from `board_close_done` is instead **warn-and-continue naming the issue** — the same contract `/fix` Step 6.3 and `/build` 4d already state for their own Done writes — with `reconcile.sh --status` / `--labels --apply` as the backstop; it is never re-driven through the bare-close path. This is the ONE exception that needs an explicit adapter call: the close is synchronous (unlike the merge arm above), so this **is** the right moment to run it. The close itself, through *any* route, makes the item read Done immediately (`issue_item` checks `state == closed` before any label) — but `board_close_done` is what actually strips the `fnd:status:*` label and the `fnd:host/session:*` claim stamp (idempotent from any state), and routing through it fires the adapter's own write-through cache invalidation internally (`_board_cache_dirty_after_write`, board.sh ~:849), so no hand-rolled `cache_dirty`/guarded `lib/cache.sh` source is needed here anymore. Without this call every spike close strands both labels on the issue. Record **resolved (verdict)**. Then reclaim any worktree in-lane the same way: `workflows/scripts/build/worktree.sh remove "$repoRoot" <slug>` (idempotent `REMOVED`/`NOT_FOUND` — safe no-op if the spike path built none). <!-- cite: SW.8 class:silent-noop-false-fix -->
- **`escalations: [{ slug, kind, payload }]`** (the worker hit a question / blocker — `blocked` / `design-fork` / `failed` / a machinery escalation) → **PARK-AND-CONTINUE** (never a modal halt):
  1. `workflows/scripts/build/worktree.sh remove "$repoRoot" <slug>` — the workflow leaves an escalated worktree standing (it removes none), so reclaim it here — and **first in this branch**, because this call is what produces the verdict step 3's comment has to report. It **no longer destroys unlanded work** (temperloop#1699/#1725/#1730): `cmd_remove` runs the unlanded-work guard *inside the same call*, ahead of the force-remove and the `build/<slug>` branch delete, so a worker that committed but never pushed — the common pre-push escalation shape — has its commits *and* its dirty tree captured to a local `refs/parked/<slug>-<sha8>` ref before anything is deleted; and when that capture **fails**, the call refuses outright rather than deleting anyway. (`$repoRoot` is the path resolved in Step 0 — this session's OWN checkout, never a foreign canonical checkout, per the working-tree-ownership rule.) The verdict rides the outcome line as the fields `preserved` / `preserved_ref` / `preserved_detail` — on the ordinary `REMOVED` / `NOT_FOUND` line, and on the `REMOVE_REFUSED` line `cmd_remove` emits (temperloop#1730) when that guard came back empty-handed. **A non-zero exit from this call is not a park failure.** `REMOVE_REFUSED` exits non-zero precisely *because* it destroyed nothing, so it is the **success case for work-preservation**: this branch treats it exactly as it treats a clean `REMOVED` and keeps disposing the chunk — steps 2–6 all still run, and the only difference is which sentence step 3's comment carries. Read the three fields and carry them into step 3:
     - **`REMOVED` / `NOT_FOUND` with `preserved: true`** → `preserved_ref` is the ref step 3's **preservation sentence** names.
     - **`REMOVED` / `NOT_FOUND` with `preserved: false`** (a `not-needed:*` / `nothing-to-preserve` / `no-occupant` detail, exit 0) → the ordinary **nothing-at-risk** case: already pushed, already merged, or no occupant. Handle it silently, and step 3's comment carries **no** preservation sentence at all — a comment naming a ref that was never minted sends whoever resumes hunting for work that does not exist, which is strictly worse than saying nothing.
     - **`REMOVE_REFUSED`** (`preserved: false` with a `capture-failed:*` / `unclassifiable:*` detail) **and a non-zero exit** → `cmd_remove` **refused to destroy what it could not capture** (temperloop#1730), so nothing was deleted and **the worktree at the reported `path` and the branch `build/<slug>` are STILL STANDING** — that surviving worktree is now the only copy of the work. **Warn once — naming `<N>`, the slug and the verbatim `preserved_detail` — then continue disposing the chunk**, and thread that same detail into step 3's comment as the **capture-failure sentence** below. The warn on its own is not enough: `/sweep` runs unattended, so a line on this run's stdout is read by nobody and the issue is the only durable surface the failure can reach. Never a modal halt — this branch is `PARK-AND-CONTINUE`, and halting strands the rest of the chunk for an operator who is not there. And never a **second, hand-rolled destroy**: no `rm -rf`, no `git worktree remove --force`, no `git branch -D`, no re-run of `remove` "to finish the job" (a re-run only re-refuses). Leave the standing worktree for `worktree.sh prune`, whose `SKIPPED_UNMERGED` / `SKIPPED_FRESH` / `SKIPPED_DIRTY` refusals — and, once a later `create` sidelines it, its `SIDELINED_WT` report — exist to hold exactly that case.
     - **`{"outcome":"ERROR"}` and a non-zero exit** → `remove` failed before or outside the guard. Treat it exactly as the refusal above: warn, thread the `error` text into step 3's capture-failure sentence in place of `preserved_detail`, **destroy nothing**, continue. The worktree or its branch may still be standing, and that surviving copy is the only copy of work nothing captured.
  2. `board_set_status "$(board_item_id <N>)" "$BOARD_OPT_READY"` — move it back to `Ready` out of In Progress (the `Blocked` Status option was retired in #435; the open *question*, carried by the label added next, parks it — not a status bucket). `BOARD_OPT_READY` is one of the exported `BOARD_OPT_{BACKLOG,READY,INPROGRESS,DONE}` constants.
  3. `gh issue comment <N> -R "$repo" --body "Parked by sweep — <the question from payload: design_fork.decision/options, or questions[], or failure_reason>. Where it stands: <one line>. <the ONE sentence step 1's outcome selects — see below; omitted entirely on the nothing-at-risk case>. Once answered: on the autonomous pipeline board, unassign yourself to release it straight back to the driver; on other boards leave it as-is and the next sweep run's Phase 1 clears the label. Either way your answer is consumed."` — post the question **first**, so it is durably recorded before the "handled" markers in step 4 land. The closing line is **board-safe** (foundation #657): unassigning is the operator's baton-return gesture only where a pipeline consumer exists (stageFind today) — the pipeline autonomously drains an *unassigned* `needs-clarification` item, clearing the label so it drives again; on a board with no pipeline it tells the operator to leave the assignment (keeping #684's queue visibility) and re-enter via the next sweep.

     **The slot takes exactly one of three forms, selected by step 1's outcome.** Fill every `<…>` from step 1's own JSON, never from a guess. The host name is load-bearing in both non-empty forms rather than decoration: on the autonomous pipeline this sweep runs on the cron host, so an operator reading the comment on their laptop cannot see the ref or the path at all and has to be told which machine to look on.

     - **Preservation sentence** (`preserved: true`) — name the ref, and say which machine holds it: `Unlanded work from this attempt is preserved at <preserved_ref> on <the host name, from $(hostname -s)>, the machine that ran this sweep. That ref is LOCAL to that host's checkout and is never pushed — this repo is public and the park is unattended, so the guard deliberately has no push transport — so it is not visible from GitHub or from any other machine. Resuming this item is a fresh re-drive from the issue, not a continuation of that ref; inspect it with git show / git diff (or worktree.sh restore) on that host if you want the abandoned attempt.`
     - **Capture-failure sentence** (`REMOVE_REFUSED`, or an `ERROR` line) — this is the **durable sink** for a failure the unattended run's warn cannot deliver, so it is a **distinct sentence** in this same comment, never folded into the "where it stands" line: `Preserving this attempt's unlanded work FAILED (<the verbatim preserved_detail, or the ERROR line's error text>), so worktree.sh refused to remove anything: the worktree at <path> on <the host name, from $(hostname -s)> and its branch build/<slug> are STILL STANDING on that host, and that worktree is the only copy of the work. Nothing here has been deleted; inspect or salvage it on that host before anything reclaims it.` Copy `preserved_detail` **verbatim** — it is the classifier's own reason string, and paraphrasing it destroys the one piece of evidence that says *why* capture failed. Name `path` from the same JSON rather than reconstructing it.
     - **Nothing** (`preserved: false` on a `REMOVED` / `NOT_FOUND` line) — omit the slot entirely. Nothing was at risk, so there is nothing to point anyone at.

     **The crash window between step 1 and this comment.** If the run dies after step 1 and before this comment lands, the pointer never reaches the issue — but neither artifact is lost silently: `worktree.sh prune` reports an unreaped preservation ref as `PARKED_REF` and a still-standing worktree as `SKIPPED_*` / `SIDELINED_WT`, each keyed by the slug's trailing issue number, and `/tidy`'s stale-claim sweep is the generic backstop for the `In Progress` claim steps 2 and 5 never got to clear.
  4. `gh issue edit <N> -R "$repo" --add-label needs-clarification --add-assignee @me` — **only after step 3's comment succeeds.** This label is the open-question gate: it re-enters the next run's Phase 1, and read-time skills (`/next`) skip a `Ready` item carrying it so it is not recommended as workable until answered. The `--add-assignee @me` routes the parked item into the operator's assigned-to-me queue **at source** (matching `/triage`), so the pipeline router only parks it (`route-already-assigned`) rather than re-deriving/re-posting the question the way the retired `route-needs-input` did (foundation #684). Ordering matters: the pipeline never re-posts a missing question, so a labeled+assigned item with no question comment is silent loss — if the step-3 comment failed, do not apply the label/assign; leave it un-flagged to re-park next run.
  5. `"$RELEASE" <N> --board "$BOARD"` — a **best-effort** claim-marker clear (and, because `--board` is passed, the board-side parked-claim-stamp clear temperloop#979 added: on an issues-only board it also strips `<N>`'s own `fnd:host/session:*` stamp once steps 2–4 have moved it off In Progress — which is exactly why the marker refusal below must not be treated as a failure, since that half already ran). Its refusal is **expected, non-fatal output, never an error**: a chunk is a multi-claim window, so `release.sh`'s one-marker-per-window design correctly **refuses** to clear a non-latest marker (K#275 — the claim-held-until-Done contract). Do not retry and do not treat the refusal as a park failure — steps 2–4 already did the board half of the park, and a later adapter Done write on this issue's eventual close (or, failing that, `reconcile.sh`) mops up any marker the refusal left behind.
  6. Record the issue as **parked** (with the question) and **continue disposing the rest of the chunk**.

**What becomes of a sweep-originated preservation ref.** A parked sweep item is **re-driven fresh** on a later run — resume is a re-run from the issue, not an in-place continuation — so `/sweep` never calls `worktree.sh restore` and the preserved commit never reaches `origin/<default>`. `prune`'s **ancestry** gate (the preserved commit is an ancestor of the merged tip) therefore *structurally cannot fire* for a ref this path minted; the **disposition** gate is the one that reaps it, when the originating issue reaches a terminal (CLOSED) state. That is what the issue-number suffix on the slug is for (this Step 3's `items[].slug` rule, above): `worktree.sh prune` parses the trailing `-<N>` back out of the ref name to look the issue up. While the issue is still open the ref is *reported* (`PARKED_REF`) and held, never reaped — so the abandoned attempt stays recoverable, on that host, for exactly as long as the question it parked on goes unanswered. `/sweep` itself never deletes a preservation ref.

After every branch above, **mark the checklist entry for this issue with its terminal disposition**: `[x] #N: <title> — <merged PR#|resolved (verdict)|parked: <reason>>`. The three terminal dispositions are `merged`, `resolved (verdict)` (spike close), and `parked` (escalation). An entry that remains unchecked at report time is a hard error (see below).

Each chunk invocation is a fresh workflow process — every worker's context is discarded on return, so no state carries between issues or between chunks. Chunks are driven one after another: the next chunk launches only after the previous chunk's merge pass completes — do not pre-spawn or overlap chunk invocations, with **one sanctioned exception**: tier 2's background **chunk 1**, launched in Step 2 before the question batch (the ratified overlap, `docs/adr/0012-sweep-two-tier-parallel-execution.md`); its merge pass still runs at this layer, and no *other* chunk is ever overlapped or pre-spawned.

**Tier-2 composition (attended overlap runs only).** When Step 2 launched chunk 1 in the background, sequence Phase 2 as follows once the question batch resolves:

1. **Wait — bounded, never indefinitely — for chunk 1's completion `<task-notification>`** (it re-invokes this driver with chunk 1's `{parked, escalations}`). Do not launch any other chunk while a chunk's merge pass is pending. *Notification timing is turn-boundary semantics:* the harness delivers a task notification when the current turn ends, never literally mid-call — so a notification that arrived while the question batch was still open is processed immediately after the batch resolves, and chunk 1's merge pass runs right then. **The bound:** if, once the batch has resolved, chunk 1's notification has *not* arrived, poll the chunk's recorded task id's state on the same background-`sleep` wake pattern the per-chunk quota gate uses — launch a background `sleep $SWEEP_BG_POLL_INTERVAL` (`run_in_background: true`), on each wake check the task's state/output, and repeat up to `$SWEEP_BG_POLL_ATTEMPTS` polls total (both settings sourced in Step 0.4; `${SWEEP_BG_POLL_ATTEMPTS:-6}` / `${SWEEP_BG_POLL_INTERVAL:-120}` on a non-vendoring checkout). A poll that finds the chunk complete recovers its `{parked, escalations}` from the task output and proceeds to the merge pass exactly as if the notification had arrived. **On exhaustion, treat chunk 1 as a dropped-background escalation:** log one line (`tier-2 chunk 1 unaccounted after $SWEEP_BG_POLL_ATTEMPTS × $SWEEP_BG_POLL_INTERVAL s — treating as dropped background chunk`), annotate each of chunk 1's checklist entries `unresolved-escalated (dropped background chunk)` **without checking them** — the annotation is a diagnostic, deliberately NOT one of the three terminal dispositions — and proceed directly to Step 3.5, whose terminal-state assertion then hard-fails loudly on the unaccounted entries per its existing contract. **The Step-3.5 assertion is REACHABLE in exactly this failure case** — the bounded wait is what guarantees the driver arrives at it instead of blocking forever on a notification that will never come; an unbounded wait would make the claimed backstop unreachable.
2. Run **chunk 1's merge pass** exactly as above (it is a chunk like any other — merge the green PRs, close the verdict-only spikes, park the escalations), then the per-chunk **quota gate**.
3. Drive the **remaining clean chunks** synchronously through the loop above, gate between each.
4. Drive the **tail chunk** (the batch's answered issues) **last** — in this same run, sequenced after every chunk whose merge pass is still pending, and behind the same per-chunk quota gate as every other chunk.

Everything downstream — Step 3.5's assertion, Step 3.6's emit, Step 4's report — is tier-agnostic: a tier-2 run's issues carry the same three terminal dispositions and the same checklist entries as a tier-1 run's.

**5-hour quota gate — per chunk: after each chunk's merge pass, before the next chunk.** Once a chunk fully lands (every issue in it merged / closed / parked) and **before launching the next chunk's invocation**, run the decision script resolved in Step 0. **Skip the gate when `$quotaGate` is empty** (no gate script in this checkout) or after the **last** chunk — both are no-ops (proceed):
```
"$quotaGate"            # the guarded path from Step 0; skip if empty
```
Branch on the verdict (identical semantics to `/build` Step 4e-quota):
- **`proceed`** / **`unavailable`** → continue to the next chunk. **`unavailable` fails open** — a missing/stale/unreadable quota snapshot never pauses the run.
- **`pause`** (remaining 5h quota < `BUILD_QUOTA_PAUSE_PCT`) → **pause and auto-resume in-session:** note the pause in the Step 4 report state (and, optionally, a one-line comment is unnecessary — singletons have no plan note), launch the wait as a **background** `sleep <wait_secs>` (`run_in_background: true`; foreground `sleep` is blocked), end the turn announcing the resume time, and on the wake re-invocation re-run `"$quotaGate"` (loop while still `pause`) then continue with the next chunk. The chunk boundary is the clean, state-consistent pause point — no chunk (and so no issue) is left half-driven by a pause. Skip the gate after the **last** chunk (nothing left to protect → fall through to Step 4).

**`--dry-run`:** STOP before Phase 2's first invocation. Print the singleton pool, which issues carry open questions (Phase-1 detection only — no `AskUserQuestion`, no label writes), and the Phase-2 set that *would* be driven **plus its chunk partition** (chunk width `$SWEEP_FANOUT_WIDTH`, hotspot deferrals called out). On a run where tier 2 *would* activate (attended, width > 1, background invocation available), additionally print which issues **would form chunk 1** (the background overlap launch) and which **would go to the question batch** — but launch **nothing**: `--dry-run` stays zero-mutation, so no background invocation is ever made under it. End with "Re-run without `--dry-run` to execute." Zero mutation.

## Step 3.5 — Pre-report terminal-state assertion

Before emitting the Step-4 report, assert that **every entry in the Phase-2 TaskCreate checklist is checked** (has a recorded terminal disposition) — **every Phase-2 issue across all chunks**, since the checklist was minted flat over the whole set before partitioning, an issue lost at a chunk boundary (dropped from a chunk's `items[]`, or left undisposed by a per-chunk merge pass) fails this assertion exactly as a skipped issue did in the sequential shape — and that includes a tier-2 **background chunk 1 whose completion notification never arrived or was dropped**: its issues are checklist-tracked identically, so a lost background chunk surfaces here as unchecked entries rather than silently vanishing (the assertion is the tier-agnostic backstop the overlap tier leans on — reachable in that failure case precisely because the tier-2 wait on the notification is **bounded**, Step 3's composition item 1, never an indefinite block). Specifically: <!-- cite: SW.9 class:false-complete-report -->

1. For each checklist entry, confirm it carries one of: `merged`, `resolved (verdict)`, or `parked`.
2. **Any unchecked entry is a hard error** — do NOT emit the Step-4 report. Instead, surface loudly:

   > **SWEEP ABORT — terminal-state assertion failed.**
   > The following Phase-2 issue(s) have no recorded terminal disposition: #N `<title>`, …
   > This indicates a bug in the sweep loop (an issue was silently skipped). Investigate before re-running.

   Stop. Do not attempt partial-report output. The hard stop makes a silent skip visible immediately rather than propagating a false-complete signal downstream to the board or operator.

This assertion is the structural guarantee that makes silent skips impossible: the TaskCreate checklist is the worklist-lock; the pre-report check is the key.

## Step 3.6 — Emit the run telemetry record

`/sweep` has no plan-note footer, so without an explicit emit here a whole run — or a run that silently stopped emitting — produces **no** signal at all (the June silent-failure class: a never-written stream is indistinguishable from "nothing to do"). Once Step 3.5's assertion passes (every Phase-2 entry has a terminal disposition), append ONE command-run record from the checked-off Phase-2 checklist — this call is the executable emit point, not a prose reminder, and its presence is mechanically enforced by `workflows/scripts/validate-command-run-emit.sh` (wired into `scripts/quality-gates.sh`), which fails CI if this invocation is removed: <!-- cite: SW.10 guard:workflows/scripts/validate-command-run-emit.sh -->

```bash
"$(git rev-parse --show-toplevel)/workflows/scripts/emit-command-run.sh" \
  --command sweep --board "$BOARD" \
  --items-processed <Phase-2 checklist size> \
  --merged <count of "merged" terminal dispositions> \
  --resolved <count of "resolved (verdict)" terminal dispositions> \
  --parked <count of "parked" terminal dispositions>
```

**The three counts MUST partition `--items-processed`.** Step 3.5 already asserts every Phase-2 entry reached exactly one of `merged` / `resolved (verdict)` / `parked`; this is that assertion carried into the record, and the emitter enforces it (`merged + resolved + parked == items_processed`, else a non-zero exit naming the arithmetic). Before temperloop#1084 there was no `--resolved` at all, so a spike closed on its verdict had to be folded into `--merged` or dropped — the latter is what happened, producing records like `{items_processed:30, merged:27, parked:1}` in which two items were simply unaccounted for. If this doc's terminal set ever gains a fourth disposition, add its field to the emitter in the same change: padding `--items-processed` to make the arithmetic close is exactly the silent under-report the check exists to prevent.

Resolve the script bare repo-relative (same convention as `workflows/scripts/build/build.config.sh` in Step 0.4) — if it is absent from a non-vendoring checkout, the `"$(git rev-parse --show-toplevel)/…"` path simply fails to execute; treat that as a no-op and continue (never let a missing/failing emit block or delay the report). The script stays `|| true`-safe for *infrastructure* failure: a write failure warns to stderr and exits 0. Exit 2 is the one loud case — the accounting mismatch above; recount and re-emit, but never let it block or delay the report.

## Step 4 — Report

End with a re-orientation block. **The report must enumerate a terminal disposition for every Phase-2 pooled issue** — no issue may appear without one of `merged`, `resolved (verdict)`, or `parked`. (This is automatically satisfied when Step 3.5 passes.) For sweeps larger than five singletons, list all issues in a table (issue# | title | disposition | detail) so the operator can scan the full set at a glance rather than inferring coverage from aggregates.

- **Fixed** — each issue → its merged PR#.
- **Resolved (verdict)** — each spike issue → closed-directly note.
- **Parked (open question)** — each issue → the question / reason it parked, and that it carries `needs-clarification` for the next run's Phase 1.
- **Skipped (flagged, unanswered)** — issues left flagged in Phase 1 (deferred / operator-absent). These are NOT Phase-2 pool members (they were filtered out in Step 2) and thus not subject to the Step-3.5 assertion — but they must still appear in the report.
- **Skipped (funnel-escalated)** — Ready singletons carrying `funnel-escalated` dropped from the fix pool in Step 1 (a stuck 5c code item with an open/failed PR awaiting the operator's manual merge/close, #697). NOT Phase-2 members; report them so the operator sees they were surfaced, not silently swept.
- One line: parked + skipped issues re-enter the next `sweep` run's Phase 1 automatically (the self-healing loop).

End with a compact **refs legend** (qualified issue/PR numbers → title) per the communication conventions.
